# Requested fixes only

This build starts from the uploaded `feature-theme-styling-v2` ZIP and preserves its visual styling.

Applied changes are limited to:
- remove Create Campaign from dashboard sidebar
- restore real campaign donation feed while preserving the existing campaign page layout
- dashboard landing data/charts and logged-in user greeting
- campaign overview chart functionality
- themed login sidebar at /login
- authentication state persistence required for the dashboard greeting

No homepage component, homepage styling, global CSS, theme configuration, checkout styling, or unrelated UI file was changed.
